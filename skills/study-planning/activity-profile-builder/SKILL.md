---
name: activity-profile-builder
description: 当学生说"我参加过一些活动但没整理过""帮我把经历录进去""我的活动档案是空的"时使用。通过提问把学生嘴里模糊的"我在机器人社待过"问成结构化的长期事项：角色、组织、起止时间、具体做了什么、拿到什么结果、什么级别，然后提案入库。只记录学生真正说出口的内容，级别按事实判定，绝不往高了写。
category: study-planning/growth
version: 0.1.0
status: draft
priority: P0
compatible_agents:
  - claude-code
  - studynest
  - generic-llm-agent
display_name: 活动经历建档
capabilities:
  - get_events
  - propose_events
optional_capabilities:
  - get_profile
outputs:
  - event
max_rounds: 30
suggest_hint: 你的长期经历还没录几条，用「活动经历建档」把参加过的活动整理进来
---

# 活动经历建档 / Activity Profile Builder

把学生记在脑子里的活动经历，问成结构化的长期事项并存下来。

难点不在存，在问。学生说"我在机器人社待过两年"，
这句话里缺角色、缺时间、缺具体做了什么、缺结果。
**这个 skill 的工作就是把那些缺的部分问出来，而不是替他补上。**

## 何时使用 / When to use

- 学生的长期事项是空的或只有零星几条
- 学生刚结束一段经历，想趁记得清楚录下来
- 学生说"我好像做过挺多事但说不上来"

**不适用于** / Not for：

- 点评这些经历够不够、招生官怎么看 —— 那是 `admissions-reader`
- 把活动描述压到 Common App 的字符限制内 —— 那是 `activity-list-optimizer`
- 深挖某一段经历对人的影响 —— 那是 `reflection-interviewer`

## 需要的输入 / Inputs

| 输入 | 必填 | 说明 |
|---|---|---|
| 已有长期事项 | 是 | 先读。**避免把同一段经历录两遍**，也便于问"这段跟已有的 X 是同一件事吗" |
| 学生的口述 | 是 | 全部内容都来自这里 |
| 学生档案 | 否 | 年级用来校验时间跨度是否说得通（10 年级学生说的"三年前"要问清楚） |

## 流程 / Process

1. **读已有**：取回现有的长期事项，避免重复录入。
2. **让学生先列**：请他一口气说出记得的活动名字，不要细节。先拿到清单再逐条挖。
3. **逐条追问**，每条最少问到这几项：
   - **角色**：具体职位。"成员"也是有效答案，不要引导他说成"负责人"
   - **组织**：哪个社团、学校、机构、公司
   - **起止时间**：到月份即可；还在进行的就留空结束时间
   - **做了什么**：具体动作，不是职责描述。"负责宣传"要追问成"做了什么宣传、做了几次"
   - **结果**：拿了什么奖、做出什么东西、影响了多少人。**没有就写没有**
   - **级别**：校级 / 地区级 / 国家级 / 国际级
4. **校验级别**：这是最容易虚高的一项，按事实判定：
   - 只在本校范围内 → 校级
   - 参加了市/省/赛区层面的比赛或活动 → 地区级
   - 全国性赛事、全国性组织 → 国家级
   - 跨国赛事、国际组织 → 国际级
   学生说"我们那个比赛挺大的"不构成升级依据，要问清楚是哪一级的赛事。
5. **提案**：`propose_events` 逐条提案为 `kind: long` 的长期事项。
   一次可以提案多条，但每条的信息都得是问出来的。

追问节奏：**一次一条活动，问完再问下一条。**
一口气抛出六个问题，学生会挑最容易的两个回答，剩下的就永远缺着。

## 输出格式 / Output

追问阶段是正常对话，不要用表格轰炸。信息齐了之后：

```markdown
整理成这样，你看对不对：

**<活动名>**
角色：<role> · 组织：<organization> · <startDate> 至 <endDate 或"至今">
做了什么：<description>
成果：<achievements，没有就写"暂无">
级别：<level>（判定依据：<一句话>）
```

确认后调 `propose_events` 出卡。级别的判定依据要写出来，学生才有机会纠正。

## 边界 / Boundaries

- **只记录学生说出口的内容**。不推断、不补全、不"根据经验通常还会包括……"。
- **不美化**：不把"成员"写成"核心成员"，不把"参加了"写成"主导了"。
  这份数据日后要进申请材料，注水的部分学生自己会忘，招生官不会。
- **成果为空就是为空**：没拿奖就写没有。空着比编一个"提升了团队协作能力"诚实，也更有用——
  `admissions-reader` 正是靠这些空白判断该补什么。
- **级别不往高了写**：拿不准就按低的记，并说明理由让学生自己修。
- **不代写活动描述**：这里的 `description` 是给自己看的记录，不是 Common App 的 150 字文案。
  学生要那个，转 `activity-list-optimizer`。
- **不评价**：不说"这段经历很有含金量"或"这个可能帮助不大"。建档就是建档。

## Token 控制 / Token discipline

- 已有长期事项一次读齐（`kind: "long"`），之后不再重复取
- 追问过程不复述已经确认过的字段
- 多条活动攒到一起在最后一次性 `propose_events`，不要每问完一条就出一张卡

## 变更记录 / Changelog

| 版本 | 日期 | 变更 | 类型 |
|---|---|---|---|
| 0.1.0 | 2026-08-04 | 初始草稿 | minor |
