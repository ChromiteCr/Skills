# 审计与新 skill 提案

生成日期 2026-08-21 · 库版本 0.16.0 · 已建成 30 个 skill

由 7 个并行 Agent 产出后经人工复核：3 路只读审计（coding-helper+skill-authoring / modeling 九个 / study-planning+songwriting+writing 十五个），4 路点子（物理两路、摄影一路、AI 使用一路）。

**这份文档里的结论分三档，请按档位区别对待：**

| 档 | 含义 |
|---|---|
| **已核实** | 我用 grep / 读文件亲自验过，行号和事实可信 |
| **待核实** | Agent 说的，看起来合理，但我没逐条验 |
| **已否决** | Agent 报了但我验完认为不成立，附理由。**不要照着做** |

> 审计 Agent 至少出过一次假引用（见第一部分 D 节），所以未标"已核实"的条目动手前请先自己看一眼原文。

---

## 第一部分：现有 skill 的优化清单

### A. 真问题，值得动

#### A1. 硬编码的易变事实没有出处也没有日期 — **已核实**

两个 skill 把会逐年变动的外部事实写死在正文里，既没有来源也没有记录日期：

- `activity-list-optimizer` SKILL.md:57–63 与 description 里写死 Common App 字符限额（职位 50 / 组织 100 / 描述 150）。脚本 `check_activity_limits` 负责数字符是对的，**但上限本身是写死的常量**，Common App 几乎每季都可能调。
- `application-timeline-builder` SKILL.md:82–88 写死提前量（推荐信 4–6 周、主文书 3–4 周、补充文书 2–3 周、送分 2–3 周、自查 3–5 天）。这条比上一条轻，因为表格旁边写了"学生的实际情况优先"，属于经验值而非硬事实。

**建议**：给这类常量加一行「核对日期 + 出处 URL」，并在 changelog 里约定"上游变了就发 PATCH"。这正是 `keynote-deck-builder` 用 A/B/C 分档解决的同一类问题。

#### A2. `modeling-problem-reading-coach` 完全没有引用 `_shared/` — **已核实**

modeling 九个 skill 里，八个引用 `_shared/` 文档 3 到 5 次，只有它是 **0 次**：

```
modeling-code-builder            5
model-critique-coach             4
model-selection-tutor            4
modeling-assumption-builder      4
paper-enhancement-builder        4
paper-structure-coach            4
team-role-coach                  3
latex-paper-formatter            1
modeling-problem-reading-coach   0   ← 孤儿
```

它是整条工作流的**第一步**，读题阶段就该开始给证据打标签。建议至少引用 `modeling-work-contract.md`（evidence_status 标注）与 `validation-playbook.md`（量纲与单位检查）。

#### A3. `writing-rules` 与 `zlc` 的互斥只写了一边 — **已核实**

`zlc` 里提了 `writing-rules` 两次，`writing-rules` 里提 `zlc` **零次**。两个 skill 的风格正好相反（一个专门禁升华腔，一个专门写升华腔），只有单向声明意味着从 `writing-rules` 那侧进来的用户不会被告知存在冲突。

**建议**：在 `writing-rules` 的边界一节补一句对称声明。这是 PATCH 级改动。

#### A4. 该用脚本而没用脚本的地方

| skill | 现在靠模型做 | 脚本能确定性完成什么 | 档 |
|---|---|---|---|
| modeling 全类 | 量纲一致性人工审（`modeling-assumption-builder` 里有专门章节） | 读变量表 + 解析方程，用 sympy 查量纲；跨章节符号冲突检测（同一个 λ 在不同节表示不同东西） | 待核实 |
| modeling 全类 | Run Manifest 字段完整性靠自觉 | 校验 `code-reproducibility-checklist.md` 规定的必填字段（run_id / 代码版本 / 环境 / 种子 / 输出哈希） | 待核实 |
| `ui-design-system-builder` | 反默认清单靠模型自查 | 解析产出的 CSS，检查是否又回到 Inter / #3b82f6 / 8px 圆角 / 那条标准阴影；对比度按 WCAG 实算 | 待核实 |

`latex-paper-formatter/scripts/check_latex.py` 和 `lyric-doctor/scripts/lyric_check.py` 已经是这条路的成功先例。

#### A5. 边界模糊的三组 skill

| 组 | 用户会怎么迷路 | 档 |
|---|---|---|
| `reflection-interviewer` ↔ `activity-profile-builder` | 都从"想记录一下这段经历"触发；一个做深度反思访谈，一个做结构化清单 | 待核实 |
| `model-selection-tutor` ↔ `modeling-assumption-builder` | 手上有草稿模型又拿不准的人，两个都像 | 待核实 |
| `paper-structure-coach` ↔ `paper-enhancement-builder` | "帮我改进论文"两个都命中；一个重排已有内容，一个规划要补的新工作 | 待核实 |

**建议**：每组在两边的「不适用于」里各加一句判别句，形式参考 `zlc` 里那句"与 `writing-rules` 互斥"。

### B. 小修

- `radio-quote-card` 的 description 里有一条触发语是 `生成 XX RADIO 的图`，`XX` 是占位符不是人话，换成「做一张车队无线电那种语录卡」。**待核实**
- `skills/.DS_Store` 触发校验脚本 WARN。加进 `.gitignore` 并删掉。**已核实**
- modeling 九个的测试用例都缺两类：**矛盾输入**（两份互相打架的题意）和**边界违规**（用户直接要求它违反自己的"不代写"）。`keynote-deck-builder` 的 Case 4 是后者的范本。**待核实**

### C. 其实没问题，不用动

写在这里是为了**防止过度施工**——审计 Agent 天然倾向于给每个 skill 都找出点毛病。

| skill | 判断 |
|---|---|
| `skill-creator` (0.3.1) | 流程清楚，用例覆盖了"改已有技能"和"从上文抠答案"两种。纯教练型 skill 不需要脚本 |
| `lyric-doctor` (0.1.0) | 已有脚本兜确定性部分，正文明确划分了"脚本管机械、模型管语感"。0.1.0 但成熟度不像 |
| `zlc` (0.1.0) | 公式够紧，失败模式表列了六种写坏的方式，边界防住了冒充和群体攻击 |
| `writing-rules` (0.2.0) | 15 条规则加禁用词表，严谨度已经是 1.0 的料 |
| `activity-profile-builder` (0.2.0) | "拆成表格、缺项标待补、拿不准按低的记"这套已经很实用 |
| `weekly-study-review` (0.2.0) | 判据只有一条"下周会因此做什么不一样的事"，够狠且可测 |
| `keynote-deck-builder` (0.3.0) | 本仓库的参照标准 |

### D. 我否决的审计结论

| Agent 报的 | 为什么不成立 |
|---|---|
| `launch-summary-panel` SKILL.md:210 有 bento 格数上限、:214 有 7:1 对比度要求，缺出处 | **假引用。** 该文件只有 182 行，不存在 210/214 行；这两条规则实际在 `keynote-deck-builder/SKILL.md:209` 和 `:214`。已 grep 确认原文件里根本没有这两条 |
| `maestrwave-ui-system` 应该补 references 解释"为什么是五档字号" | 这是从一个真实在跑的项目里抽出来的既成视觉系统，五档就是那个项目的事实。要求它为自己的既定取值找外部文献是把 `keynote-deck-builder` 那套套错了地方 |
| `launch-summary-panel` / `ui-design-system-builder` 的圆角、间距等数值缺文献依据 | 同上。设计取值是选择不是主张，不需要引用。`keynote-deck-builder` 需要引用是因为它在**断言别人的做法**（"苹果不低于 30pt"），性质不同 |
| 摄影那路："我 8 个点子没有一个不值得做" | 没有回答我的问题。见第二部分我自己的否决清单 |

---

## 第二部分：新 skill 提案

### 物理（第一优先，数量最多）

物理类的目标是：能把**所有**物理问题（计算题、建模题、科研问题、IYPT 开放题）拆开。以下按"拆解一条物理问题的完整流水线"组织，五组正好对应动手的先后顺序。

#### 组〇：入口分流（最先碰到）

| name | 一句话 | 确定性部分 | 重复风险 |
|---|---|---|---|
| `physics-problem-router` | 统一入口：先问三件事——这是哪类问题（计算/建模/科研/IYPT）、目标产物是什么（数值答案/可检验模型/机制解释）、卡在哪一步，再分流到下游对应 skill；**防止用户一开始就进错门** | 检查分流所需的三个字段是否齐全；检出"问题类型与所求 skill 不匹配"的情况 | 无 |

#### 组一：从题面到可解结构（动笔之前）

三个目标的分工：拆解物理原理 = 每条条件追到哪条定律、为什么适用；挖掘物理本质 = 找出不变量、对称性、被理想化掩盖的东西；分析物理过程 = 把过程分阶段、标出每段的守恒量与突变点。

| name | 一句话 | 确定性部分 | 重复风险 |
|---|---|---|---|
| `competition-scenario-extractor` | 把冗长原始材料（竞赛题、文献段落、实验描述）压成干净题面 + 子问题树；**每个可忽略项标注它掩盖了什么物理机制、近似在什么条件下会破** | 标出模糊措辞、缺失约束、题面内自相矛盾的量纲 | 无 |
| `problem-formalization-coach`（主拆原理） | 题面拆成已知、未知、约束；**每条条件强制追到背后的定律并写明该定律在此处的适用条件**；每个假设标明它近似掉了什么物理，**不解题** | 检查字段完整；检出凭空补进来的量；检出"用了定律但没说适用条件"的条目 | 与 `modeling-problem-reading-coach` 需划界 |
| `problem-representation-scout`（主分析过程） | 诊断卡住是因为缺哪个表示；**先把过程拆成阶段（哪段守恒、哪段突变、阶段间以什么衔接），再为每阶段选配表示**（受力图、能量柱状图、v-t 图、电路重画） | 阶段拆分后可核对各阶段首尾状态量是否衔接 | 无 |
| `reference-frame-choice-guide`（主挖本质） | 帮选参考系并说清为什么；**强制列出换系后不变的量（守恒律、标量、相对量）与变了的表象**，让人看见本质与坐标选择的差别；估各系下的代数复杂度 | 可符号比较不同系下表达式项数；核对"不变量清单"里的量是否真不变 | 无 |
| `physics-mechanism-decomposer`（挖本质主将，IYPT/科研/建模的命门） | 开放现象拆成机制清单：列出所有候选机制 → 逐个给出标度律（它怎么随控制参数变化）→ 用量级比较排出主导/次导/可忽略 → 给每个机制配一个能分辨它的观测量。**这是把"为什么会这样"变成可检验命题的一步，是所有开放题绕不开的动作** | 各机制标度律的量纲一致性；量级比较可复核；机制清单完备性自检（能量/动量/物质收支有没有对不上的项） | 与 `fermi-estimation-coach` 需划界：它排机制，fermi 估数值 |

#### 组二：动手时的纪律（写推导、做估算）

| name | 一句话 | 确定性部分 | 重复风险 |
|---|---|---|---|
| `concept-to-formula-deriver`（拆原理的执行工具） | 从守恒律或定义重建标准公式，每步标物理含义；**重点是"为什么是这条守恒律/这个定义"——把公式还原成原理的推论，而非背下来的结论** | 终式与标准式比对 | **边界要写死**：可以讲公式从哪来，作业推导本人做 |
| `symbolic-first-discipline-coach`（挖本质的执行工具） | 并排展示符号做到底与早代入数字的差别；**符号形式让结构可见：哪个量真正无关、哪两个量只以组合形式出现（即本质自由度更少）** | 符号解与数值解逐步对比；符号化简后自动检出消去的参数与剩余独立组合 | 无 |
| `fermi-estimation-coach` | 结构化数量级估算：**机制排序已由 mechanism-decomposer 给出时，本 skill 只负责给每个机制配上数值锚点并合成量级结论**，每步带合理性检查 | 检查中间估值是否落在已知量级区间；核对引用的机制链与估值是否闭合 | 无 |

#### 组三：检错（最便宜的防线）

检错不是挑代数毛病，而是反向逼问本质：每个错误都对应一条被违反的原理、一个被认错的物理过程。

| name | 一句话 | 确定性部分 | 重复风险 |
|---|---|---|---|
| `dimensional-analysis-checker` | 逐项查量纲，抓符号错、漏项、多余项；**每个剩余项必须能说出它代表什么物理贡献，说不出的项就是没理解的项** | sympy 量纲检查；列出该约掉却没约掉的项；检出"量纲对但物理上不该出现"的参数组合 | 无 |
| `limiting-case-validator` | 查公式在 m→0、v→c、θ→0 等极限下是否物理合理；**极限的选择本身就是在问"这个系统的本质由哪个参数控制"，回归已知特例（牛顿极限、静态极限）即验证原理接对没有** | sympy 求极限、提主项、查符号；自动枚举题目涉及的小参数/大参数候选极限 | 无 |
| `answer-plausibility-checker` | 数值答案先过嗅觉测试再信：量级、极限、单位、与已知参考值比；**对科研/建模类无标准答案的问题，改用"数量级 + 守恒律总量核算"双锚点** | 量级估算 + 常见物理量参考值表；全程能量/动量/电荷总量收支表 | 与上两条部分重叠，可考虑三合一 |
| `derivation-step-checker` | 逐步验推导：每一步不仅要代数对，**还要标注用的是哪条定律、该定律在此步的适用条件是否仍成立**（变质量、非惯性系、准静态假设中途被悄悄破坏是高频翻车点） | sympy 逐步代数验证；逐步单位守恒与符号翻转检查 | 无 |

#### 组四：数据与模拟（科研/IYPT 的实证环节）

| name | 一句话 | 确定性部分 | 重复风险 |
|---|---|---|---|
| `uncertainty-propagator` | 不确定度传播（含相关项），给灵敏度系数；**灵敏度排序直接回答"本质由哪个量控制"，引导下一轮测量把预算花在最敏感的观测量上** | sympy 求导 + 蒙特卡罗对标；查雅可比条件数 | 无 |
| `model-fit-auditor` | 拟合诚实性体检：残差、χ²/自由度、过拟合信号、异常点处理；**残差结构是理论模型的照妖镜——系统性残差=模型漏了物理过程，必须说出漏的可能是哪一段机制** | scipy 算标准化残差、leverage、影响函数、AIC/BIC；残差对各自变量扫描出结构化模式 | 无 |
| `dataset-systematic-error-hunter` | 从原始数据里找藏着的系统误差（温漂、零点漂移、非线性）；**每种系统误差归因到一个具体的物理过程（热学、电子学、机械），而非一句"数据有问题"** | 残差对时间/温度/参数的趋势、首尾漂移、重复不一致 | 无 |
| `numerical-stability-auditor` | 判模拟结果可不可信：能量守恒、步长收敛、鬼频检测；**守恒量漂移曲线本身就是过程分析工具——在哪一步开始漂移，就指向哪个物理过程被离散化破坏** | 能量相对漂移、变步长收敛率、功率谱找离散化鬼峰 | 无 |

**先做哪三个**：`dimensional-analysis-checker` → `limiting-case-validator` → `physics-mechanism-decomposer`。前两个确定性最强（sympy 直接跑）、见效最快，用来验证"物理类适不适合做成 skill"；第三个是流水线里唯一覆盖开放题核心动作（机制分解）的，原推荐里的 `model-fit-auditor` 只服务有数据的子集，推迟到实证环节铺开时再做。

### 摄影（第二优先）

摄影类重新定位为：**围绕一张已拍好的照片做"再创作与呈现"**——提炼、装裱、成套、回顾。前提是诚实：Agent 看不到画面的美学意图，所以每个 skill 的分工都是"模型负责提炼与版式判断，脚本负责绘制与渲染"，凡需要"看懂照片"的步骤都由人做终审。

| name | 一句话 | 确定性部分 | 判断 |
|---|---|---|---|
| `photo-poster-stylist` | 从照片提炼核心视觉元素（主体轮廓、主色板、明暗结构），产出**极简风格化海报**：模型把画面描述转译成 SVG 图形语言（几何化处理、留白、最多三色），脚本渲染成图；版式规则（字重层级、网格、出血）写成 references | 调色板数量与对比度可脚本校验；SVG 元素数设上限强制"极简"；输出尺寸/分辨率确定性生成 | **核心项。** "提炼什么、怎么几何化"是判断活，正是这个库该写下来的；绘制交给代码，不假装会画画 |
| `photo-exif-frame` | 在照片下方加自定义颜色的边框带，排版照片信息（光圈、快门、曝光补偿、ISO、拍摄时间、设备）；色值、字体、字段可选、边距比例全部可配 | PIL 读 EXIF + 绘制边框与文字，纯确定性；EXIF 缺字段时明确降级（留空而非编造） | **核心项。** 几乎零判断、全确定性，最快能做成的一个；版式默认值写进 references |
| `photo-series-layout` | 把一组照片（3–9 张）排成一页成套作品：统一画幅、间距、顺序叙事（时间/明暗/冷暖），输出单张长图或 PDF；适合组照、系列作品的呈现 | PIL 拼接 + 尺寸归一化；排版的网格与间距规则确定性执行 | 值得做。与 `photo-exif-frame` 可组合（每张先装裱再成套），顺序建议由人定、模型只给候选 |
| `shoot-outing-review-card` | 一场拍摄结束后自动生成一张"回顾卡"：封面图 + 参数分布小结（焦距/光圈/快门各集中在哪）+ 时间线，类似年度歌单；**目的是帮摄影师看见自己的习惯** | PIL 读 EXIF 聚合统计 + 模板渲染，全确定性 | 值得做。继承原 `exif-habit-diagnoser` 的统计内核，但产物从 CSV 报告变成可分享的视觉卡 |
| `photo-caption-writer` | 为照片写图注/作品阐述：短（一句话说明何时何地何参数）与长（一段创作意图）两档；**边界写死：只基于拍摄者提供的背景与 EXIF 事实写，不编造"想表达的情感"** | EXIF 事实字段由脚本注入，保证参数不瞎编 | 值得做。判断活在"详略与语气"，事实部分脚本兜底，正好是这个库擅长的分工 |

### AI使用

设计原则：每个 skill 只解决一个具体痛点，脚本能兜底的绝不靠模型自觉。按"新手 → 进阶 → 专业开发者"排序。

| name | 一句话 | 确定性部分 | 面向 |
|---|---|---|---|
| `ai-output-fact-checker` | 把 AI 的回答拆成可检验声明（链接、库名+版本、论文 DOI、命令行参数），逐条给出验证方法；**防住"引用了不存在的库/论文/命令"这种直接复制就会炸的事故** | URL 活性检查、包版本与发布时间在 PyPI/npm 上校对、DOI 解析 | 所有人，**推荐先做**——确定性最强 |
| `prompt-brief-builder` | 新手向：把"帮我写个东西"这种一句话需求，通过 3–5 个追问补成一份完整任务简报（目标、受众、约束、验收标准、不要什么），再给 AI | 简报模板字段完整性检查（脚本可验） | 新手 |
| `ai-answer-triage` | 新手向：教"AI 给的答案哪部分能直接用、哪部分必须先验证"——按声明类型分级（常识/可查证事实/观点/代码），每级配一句处置动作 | 声明类型分级的规则清单；代码类声明一律标"必须实跑" | 新手 |
| `ai-code-onboarding-checklist` | 拿到 AI 生成的代码先过一遍客观体检再决定信不信：行数、嵌套深度、有没有测试、有没有硬编码密钥/TODO、依赖是否真实存在；**老实写明：它查不了算法正确性** | 行数/嵌套/密钥正则/依赖存在性全部脚本可跑 | 进阶 |
| `ai-session-handoff-writer` | 长任务跨 session 时，把当前进度压成一份交接文档（已做什么、关键决定及理由、待办、坑），让下一个 session 的 AI 或人能直接接手；本库已有 `handoff-template.md` 可作底 | 交接文档必填字段检查（对应 Run Manifest 思路） | 进阶 |
| `ai-diff-review-protocol` | 专业向：AI 改完代码后，按"意图是否匹配 → 边界条件 → 副作用面 → 可逆性"四步走查 diff，每步有明确的放过/打回判据；**不逐行精读，只盯高风险点** | diff 统计（改动文件数、增删行、是否动了测试/配置/迁移文件）脚本生成，超阈值强制人工逐段过 | 专业开发者 |
| `ai-generated-test-auditor` | 专业向：AI 写的测试常"断言恒真/只测快乐路径/复制实现逻辑"。体检三个信号：断言与实现是否同源复制、有没有边界与异常用例、删掉被测代码后测试是否真的变红 | 突变测试抽样（脚本随机注释一行实现跑测试，验证测试真能抓错） | 专业开发者 |

## 事项

1. **物理、摄影单开一个 category**