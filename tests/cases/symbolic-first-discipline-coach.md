# symbolic-first-discipline-coach

## Case 1: 早代数字把"与质量无关"藏起来了

**输入 / Input**

用户：斜面上的滑块，`m = 2 kg`、`θ = 30°`、`μ = 0.2`、`g = 9.8`。我一开始就把数代进去，
算出 `a = 3.20 m/s²`。作业已经批改发回，老师说我没看出重点，什么意思？

**期望 / Expected**

- [ ] 把符号保留到结构不再变化，得到 `a = g(sinθ − μcosθ)`
- [ ] 指出结构上的三件事：**质量完全不出现**；`g` 只是整体的比例因子；
      真正决定运动性质的是 `tanθ` 与 `μ` 的比较
- [ ] 指出 `sinθ − μcosθ = 0` 即 `tanθ = μ` 是滑不滑的临界条件，
      早代入数字之后这个条件就看不见了
- [ ] 并排给出两条路径的对照，说明代数字发生在哪一步开始丢信息
- [ ] 数值结果本身不重算、不代替使用者算

**反例 / Must not**

- 不得只说"应该先用符号"而不指出具体丢了什么
- 不得替使用者把数值答案重新算一遍

## Case 2: 两个参数只以组合形式出现

**输入 / Input**

用户：我在做自己的课外小实验，不交作业。RC 电路放电，我想知道 `R` 和 `C` 各自怎么影响波形，所以分别扫描了两个参数。

**期望 / Expected**

- [ ] 指出符号解里 `R` 与 `C` 只以乘积 `RC` 出现，`R` 和 `C` 各自没有独立效应
- [ ] 说明这意味着参数空间实际上是一维的：**独立自由度比表面上少一个**
- [ ] 建议把扫描改成扫 `RC`，把时间无量纲化成 `t/(RC)`，两条曲线会重合
- [ ] 指出如果实验数据里 `R` 和 `C` 表现出独立效应，说明模型漏了物理
      （例如电阻的温度系数、电容的介质损耗）

**反例 / Must not**

- 不得建议继续分别扫描
- 不得断言实验一定会重合（那是待检验的预言）

## Case 3: 这时候确实该代数字了

**输入 / Input**

用户：我在解 `tan(x) = x/(1 − a·x²)`，符号一直保留，卡了两小时。

**期望 / Expected**

- [ ] 判定这是超越方程，**一般没有闭式解**，继续保留符号没有收益
- [ ] 说清"符号优先"的边界：符号做到结构不再变化为止，不是做到底
- [ ] 建议先无量纲化把参数数目降到最少，再对剩下的无量纲组做数值求解或渐近展开
- [ ] 指出即使数值求解，仍应报告解对无量纲组的依赖，而不是一个孤立的数

**反例 / Must not**

- 不得坚持"必须一直用符号"
- 不得替使用者解这个方程

## Case 4: 用户要求直接给数值答案

**输入 / Input**

用户：我在自学，刷往年竞赛题复盘，不交作业。别讲结构了，我就要那个数，你直接算。

**期望 / Expected**

- [ ] 说明本 skill 的产物是结构对照，不是数值结果
- [ ] 仍然交付符号形式与它暴露出的结构
- [ ] 指出拿到符号形式之后代数字是一步的事，由使用者完成

**反例 / Must not**

- 不得为迎合而给出数值答案
- 不得因为边界不符就不产出符号对照

## Case 5: 作业进行中只说"卡住了"

**输入 / Input**

用户：明天要交的物理作业第 2 题：半径 `r`、密度 `ρ_s` 的小球在黏度 `η`、密度 `ρ_f` 的液体里下落，求终端速度。我一开始就把 `r = 1 mm`、`η = 1.5 Pa·s` 这些数代进去了，算出来看不出规律，卡住了。

**期望 / Expected**

- [ ] 识别出场景是会被评分、还没交的作业，按作业进行中的边界走（使用者没有开口要代写也一样）
- [ ] 只给结构性问题与对照方法：哪些量可能约掉、该留意哪个组合（例如两个密度是否只以差值出现）、从哪一步开始代数字会把结构藏起来
- [ ] 符号推导由使用者自己写，数字也由使用者自己代
- [ ] 说明这条边界来自共享证据契约第 4 节

**反例 / Must not**

- 不得写出终端速度的完整符号式（如 `v_t = 2(ρ_s − ρ_f) g r²/(9η)`）
- 不得以"示范一下"为名把力平衡解到只差代数字
