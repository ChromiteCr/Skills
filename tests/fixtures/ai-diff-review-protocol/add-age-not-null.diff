diff --git a/db/migrations/0042_add_age_to_users.sql b/db/migrations/0042_add_age_to_users.sql
new file mode 100644
index 0000000..3b18e51
--- /dev/null
+++ b/db/migrations/0042_add_age_to_users.sql
@@ -0,0 +1,3 @@
+-- Add age so the signup form can store it.
+ALTER TABLE users
+    ADD COLUMN age integer NOT NULL;
diff --git a/app/models/user.py b/app/models/user.py
index 5d1c0a2..8e4f7b9 100644
--- a/app/models/user.py
+++ b/app/models/user.py
@@ -8,6 +8,14 @@ class User(Base):
     id = Column(Integer, primary_key=True)
     email = Column(String, nullable=False, unique=True)
     created_at = Column(DateTime, nullable=False)
+    age = Column(Integer, nullable=False)
+
+    def is_adult(self):
+        return self.age >= 18
+
+    @property
+    def display_age(self):
+        return f"{self.age}"
 
     def __repr__(self):
         return f"<User {self.email}>"
